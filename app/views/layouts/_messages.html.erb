<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
    <div class="alert alert-danger">
      <span class="close-flash glyphicon glyphicon-remove"></span>
      <%= content_tag :div, msg, :id => "flash_#{name}" %>
    </div>
  <% end %>
<% end %>
